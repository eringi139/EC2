<h1>投稿一覧</h1>
<% @items.each do |item| %>

  <p>商品名</p>
  
  <p>￥</p>
  
  <% if item.image.attached? %>
    <%= image_tag item.image %>
  <% else %>
    <%= image_tag 'no_image' %>
  <% end %>
<% end %>