<body>
　


<h2>商品一覧(全<%= @items.count %>件)</h2>
 
 <ul>
  <% @items.each do |item| %>
  <div>
　<%= link_to item_path(item.id) do %>
  <%= image_tag item.image, width: "210px", height: "150px" %>
  <p><%= item.name %></p>
  <p><%= item.price.to_s(:delimited) %></p>
  <% end %>


</ul>
</div>
<% end %>
</body>
