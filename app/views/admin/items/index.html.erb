<h2>商品一覧</h2>

<div>
  <table>
    <thead>
      <tr>
        <th>商品ID</th>
        <th>商品名</th>
        <th>税抜価格</th>
      </tr>
    </thead>
  <% @items.each do |item| %>
  <tbody>
  <tr>
    <td><%= item.id %></td>
    <td><%= link_to admin_item_path(item.id) do %>
    <u><%= item.name %></u></td>
    <% end %>
    <td><%= item.price %></td>
    </tr>
    </tbody>
    <% end %>
    </table>
    <%= paginate @items %>
  </div>

